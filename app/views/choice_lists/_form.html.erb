<%= form_for(choice_list) do |f| %>
  <% if choice_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(choice_list.errors.count, "error") %> prohibited this choice_list from being saved:</h2>

      <ul>
      <% choice_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :key %>
    <%= f.text_field :key %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="field">
    <%= f.label :label %>
    <%= f.text_field :label %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
