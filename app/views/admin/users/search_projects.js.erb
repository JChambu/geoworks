//  revisar como hacer el onchange en ruby
// projects = "<%= j(collection_select(:project, :id, @p, :id, :name, {prompt: true}, {class: 'form-control form-control-sm', onchange: 'search_filters_for_project()'} )) %>";
// $('#projects_for_filters').html(projects);
