<%= form_for "generate", :url => generate_deliveries_url,
          :html => {:method => :get, :class => "form-inline"} do |f| %>  

      <%= f.label "Nombre Archivo" %>
      <%= f.text_field :name %>
      <%= f.submit "Generar" %>

<% end %>
