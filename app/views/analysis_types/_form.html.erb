<%= form_for(analysis_type) do |f| %>
  <% if analysis_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(analysis_type.errors.count, "error") %> prohibited this analysis_type from being saved:</h2>

      <ul>
      <% analysis_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
