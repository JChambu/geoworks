$('<%= j render "share" %>').on('hidden.bs.modal', function (e) {
  // Cuando se termina de ocultar el modal lo elimina para evitar que se quede pegado el tab
  $(this).remove();
  $('.modal-backdrop').remove();
}).modal();

// Cierra dropdown
$('#project_dropdown').dropdown('toggle')
