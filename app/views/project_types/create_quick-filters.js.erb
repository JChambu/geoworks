//Navarra.project_types.config.project_field = '<%= params[:q][:project_field] %>';
//Navarra.project_types.config.input_value = '<%= params[:q][:input_value] %>';
console.log("quick filters")

field =  "app_estado";
value = '<%= params[:q][:input_value] %>';
var filter_option = 'app_estado|' +'='+ "|'<%= params[:q][:input_value] %>'";
console.log(filter_option)
var filter_kpi = 'app_estado|' + ' ='+ "|'<%= params[:q][:input_value] %>'";
console.log(filter_kpi)
var filter_view = field  + ' = '+ "'<%= params[:q][:input_value] %>'";

Navarra.project_types.config.filter_option.push(filter_option);
Navarra.project_types.config.filter_kpi.push(filter_kpi);

// Elimina y vuelve a agregar el item al card Filtros Activos
$('#filter-body').append(
  $('<div>', { // item
    'class': 'message text-light bg-secondary px-2 mb-1 py-1 rounded border border-dark shadow',
    'id': filter_option,
    'value': filter_kpi
  }).append(
    $('<b>', {
      'text': 'Filtrado por: '+filter_view
    }),
    $('<button>', {
      'type': 'button',
      'class': 'close',
      'id': 'close_point_color',
      'text': '×'
    })
  )
)

resize_graphics();

//$("#filters-modal").modal('hide');

$(".fa-filter").css("color", "#d3d800");
var point_color = Navarra.project_types.config.field_point_colors;
if(point_color == ''){
    Navarra.geomaps.wms_filter();
}else{
    Navarra.geomaps.point_colors_data();
}
