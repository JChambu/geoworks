<% if @analytics_dashboard.errors.empty? %>
      // add the row to the table, show alert and hide modal
    $('table tbody tr#<%= @analytics_dashboard.id %>').replaceWith(' <%= j render "row", analytics_dashboard: @analytics_dashboard %>');
  $('.alert-success').html("El registro ha sido modificado con éxito").show();
  $('#ana-modal').modal('hide');
<% else %>
      // show first error
    alert('<%= @analytics_dashboard.errors.full_messages[0] %>');
<% end %>
