<%= simple_form_for @load_location, :html => { :class => 'form-horizontal', :multipart => true} do |f| %>


  <%= f.input :name %>
  <%= f.file_field(:file) %>

    <%= f.button :submit, :class => 'btn-primary' %>

<% end %>
