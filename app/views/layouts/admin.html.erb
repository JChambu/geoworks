<!DOCTYPE html>
<html>
  <head>
    <%= render :partial => 'common/head' %>
  </head>
  <body data-controller="<%= params[:controller] %>" data-action="<%= safe_action_name params[:action] %>" class="nav-sm  dashboard">
    <%= render partial: 'layouts/admin_header' %>
  </body>
</html> 
<style type="text/css">
  :root{
    --primary-color:<%= ENV['PRIMARY_COLOR'] || '#d3d800' %>;
    --primary-color-transparent:<%= ENV['PRIMARY_COLOR_TRANSPARENT'] || '#d3d80080' %>;
  }
</style>
